# Configuración de Stockeate API
# Copiar a .env y rellenar los valores

# ===== Database =====
DATABASE_URL="postgresql://user:password@localhost:5432/stockeate"

# ===== JWT =====
JWT_SECRET="tu-clave-secreta-super-segura-min-32-caracteres"
JWT_EXPIRATION="7d"

# ===== API URL (para CORS y URLs de respuesta) =====
API_URL="http://localhost:3000"

# ===== Email / Resend =====
RESEND_API_KEY="re_xxxxx"

# ===== Google Cloud Vision API =====
# IMPORTANTE: Descargar credentials.json desde Google Cloud Console
# 1. Ve a https://console.cloud.google.com
# 2. Crea una cuenta de servicio para Cloud Vision API
# 3. Descarga el archivo JSON de la clave
# 4. Guarda como credentials.json en la raíz del proyecto
# 5. Descomenta la línea siguiente:
# GOOGLE_APPLICATION_CREDENTIALS="./credentials.json"

# Para producción (Render):
# Subir credentials.json a "Secret Files" con ruta: /etc/secrets/credentials.json
# Y configurar: GOOGLE_APPLICATION_CREDENTIALS="/etc/secrets/credentials.json"

# ===== Entorno =====
NODE_ENV="development"
# NODE_ENV="production"  # Para producción
